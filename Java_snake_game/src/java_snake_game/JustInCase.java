/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package java_snake_game;

import static java_snake_game.GameBoard.MAP_SIZE;
import static java_snake_game.GameBoard.TILE_SIZE;

/**
 *
 * @author sockand
 */
public class JustInCase {
    
    
    public JustInCase(){
        /**
        if(i - MAP_SIZE > 0){
      if((tiles[i-1]==Engine.TileType.SNAKE||tiles[i-1]==Engine.TileType.SNAKE_HEAD)&&(tiles[i+1]==Engine.TileType.SNAKE||tiles[i+1]==Engine.TileType.SNAKE_HEAD)){
          g.fillRect(x * TILE_SIZE, y * TILE_SIZE + 2, TILE_SIZE, TILE_SIZE - 4);
      } 
      else if(tiles[i-1]==Engine.TileType.SNAKE||tiles[i-1]==Engine.TileType.SNAKE_HEAD){
          g.fillRect(x * TILE_SIZE, y * TILE_SIZE + 2, TILE_SIZE - 4, TILE_SIZE - 4);
      } else if(tiles[i+1]==Engine.TileType.SNAKE||tiles[i+1]==Engine.TileType.SNAKE_HEAD){
          g.fillRect(x * TILE_SIZE + 2, y * TILE_SIZE + 2, TILE_SIZE, TILE_SIZE - 4);
      }
      else if((tiles[i-MAP_SIZE]==Engine.TileType.SNAKE||tiles[i-MAP_SIZE]==Engine.TileType.SNAKE_HEAD)&&(tiles[i+MAP_SIZE]==Engine.TileType.SNAKE||tiles[i+MAP_SIZE]==Engine.TileType.SNAKE_HEAD)){
          g.fillRect(x * TILE_SIZE + 2, y * TILE_SIZE, TILE_SIZE - 4, TILE_SIZE);
      } 
      else if(tiles[i-MAP_SIZE]==Engine.TileType.SNAKE||tiles[i-MAP_SIZE]==Engine.TileType.SNAKE_HEAD){
          g.fillRect(x * TILE_SIZE + 2, y * TILE_SIZE, TILE_SIZE - 4, TILE_SIZE - 4);
      } else if(tiles[i+MAP_SIZE]==Engine.TileType.SNAKE||tiles[i+MAP_SIZE]==Engine.TileType.SNAKE_HEAD){
          g.fillRect(x * TILE_SIZE + 2, y * TILE_SIZE + 2, TILE_SIZE - 4, TILE_SIZE);
      }else {
          g.fillRect(x * TILE_SIZE + 2, y * TILE_SIZE + 2, TILE_SIZE - 4, TILE_SIZE - 4);
      }
      }else {
          g.fillRect(x * TILE_SIZE + 2, y * TILE_SIZE + 2, TILE_SIZE - 4, TILE_SIZE - 4);
      }
      * 
      * 
      * 
           // Use URL (instead of File) to read from disk and JAR.
         URL url = this.getClass().getClassLoader().getResource(soundFileName);
         // Set up an audio input stream piped from the sound file.
         AudioInputStream audioInputStream = AudioSystem.getAudioInputStream(url);
         // Get a clip resource.
         clip = AudioSystem.getClip();
         // Open audio clip and load samples from the audio input stream.
         clip.open(audioInputStream);
         
      **/
    }
}
